/*
     This class represents the data structure of a product stored in the refrigerator.
     It should receive some necessary product data via the constructor and store it in the product instance:
     - The name of the product as a string
     - The product volume as an integer (integer)
     - The expiry date of the product as a date (Date)
*/
class Product {
    
}

export default Product;